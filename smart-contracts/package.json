{
  "name": "clearrain",
  "private": true,
  "description": "Protocol for self-governed online communities",
  "version": "0.1.0",
  "author": "samhatem",
  "scripts": {
    "compile": "truffle compile",
    "test": "yarn migrate:dev && truffle test ./test/communityTest.js --network development",
    "test:rinkeby": "truffle test ./test/communityTest.js --network rinkeby",
    "migrate:dev": "truffle migrate --network development",
    "migrate:rinkeby": "truffle migrate --network rinkeby",
    "lint-fix": "solium -d contracts/ --fix"
  },
  "dependencies": {
    "@openzeppelin/contracts-ethereum-package": "^3.0.0",
    "@openzeppelin/upgrades": "^2.8.0",
    "dotenv": "^8.2.0",
    "hardlydifficult-eth": "^1.0.0"
  },
  "license": "MIT",
  "devDependencies": {
    "@openzeppelin/cli": "^2.8.2",
    "@openzeppelin/test-helpers": "^0.5.5",
    "@truffle/hdwallet-provider": "^1.0.35",
    "eth-gas-reporter": "^0.2.17",
    "ganache-cli": "^6.9.1",
    "truffle": "^5.1.25"
  }
}
